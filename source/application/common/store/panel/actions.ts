import { createAction } from '@ngrx/store';

export const INITIALIZE = createAction('[Panel] Initialize');

// export const LOAD = createAction('[Panel] Load');

export const CLOSE = createAction('[Panel] Close');

export const RESET = createAction('[Panel] Reset');

export const OPEN = createAction('[Panel] Open');

export const TOGGLE = createAction('[Panel] Toggle');
